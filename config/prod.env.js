'use strict'

const config = {
  db: {
    kvtest: {
      host: 'localhost',
      name: 'kvtest',
      user: 'root',
      password: '',
      ssl: false
    }
  },
  port: 8300
}

module.exports = config
